{
  "name": "ditto-maker",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "fetch:pokemon": "node scripts/fetch-pokemon.mjs",
    "make:portraits": "node scripts/make-portraits.mjs",
    "make:npc-portraits": "node scripts/make-npc-portraits.mjs",
    "fetch:cries": "node scripts/fetch-cries.mjs"
  },
  "dependencies": {
    "phaser": "^4.2.1"
  },
  "devDependencies": {
    "sharp": "^0.35.3",
    "typescript": "^7.0.2",
    "vite": "^8.2.1"
  }
}
